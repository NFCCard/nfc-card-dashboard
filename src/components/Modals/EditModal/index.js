import React from "react";
import ModalCore from "../Base/ModalCore";

const EditModal = ({ modalButton }) => {
	return <ModalCore modalButton={modalButton}>edit</ModalCore>;
};

export default EditModal;
